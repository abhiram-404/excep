import 'package:flutter/material.dart';

class CardiologyPage extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: Text('Cardiology'),
        backgroundColor: Colors.teal,
      ),
      body: Center(
        child: Text('Cardiology Department'),
      ),
    );
  }
}
